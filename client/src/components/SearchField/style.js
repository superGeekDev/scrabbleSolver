import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles(theme => ({
	searchContainer: {
		padding: "10px 20px",
		color: "black"
	},
	searchBar: {
		background: "#f4c984",
	}
}));